# Browser Build Verification Notes

CI run 210 (`33147833095`) passed on PR head `8d2ed2b` (packaged merge commit `e55a963`). The Windows package job imported the project, exported Windows and Web targets, structurally validated and headlessly launched the x86-64 executable, verified the complete Web payload, and uploaded `release-candidate-210` with exact provenance and SHA-256 checksums. The downloaded executable is 97,848,976 bytes with a 328,836-byte embedded PCK, and every recorded checksum revalidated locally. The job also uses checksum-pinned `rcedit`, verifies the executable's `Market of Ash` product name and `0.9.0.0` file/product versions, launches the GUI, and captures its exact 960×540 client area. Manual inspection of the final frame confirms a clean neutral Main Menu; earlier iterations exposed and fixed off-screen desktop leakage and a runner-cursor tooltip.

Three dependent jobs served that exact Web artifact with the required isolation headers in Chrome and Firefox on Linux plus Edge on Windows. Each produced 28 screenshots covering 14 required states at exact 960×540 and 1280×720 viewports, for 84 frames total. The matrix includes Main Menu, Shop, Pause, Departure, non-mutating Return-to-Shop, arrival handoff, destination Shop, protected new-game confirmation, normal and Large text variants, Gatekeeper's Chalk, and its resolved result. Each accepted frame requires the loading overlay to clear, the app-published state to match, the canvas ARIA label and polite status region to agree with the announcement, and decoded pixels to prove material transitions.

The run-200 set of all 84 images was inspected, and run 210 repeated the same automated matrix from the current packaged artifact. Primary actions, selected values, resource totals, map labels, event context, disabled explanations, arrival handoff, and confirmation choices remain readable at both sizes and across all three browser engines. The audit exposed and fixed Web-specific issues that source tests had not reached: browser-reserved navigation keys, stale pre-layout target geometry, stale destination diagnostics, a focused route response left below the scroll viewport, Firefox WebGL startup under automation, and exact Firefox viewport sizing. The final route-event captures visibly reveal the first response and the result captures keep `Enter Brine Cross` on-screen.

The capture uses trusted pointer actions against app-published control geometry for packaged-browser traversal; deterministic Godot suites separately prove keyboard and controller focus semantics. Local Selenium remains unable to start Chrome because of the host's macOS Mach-port restriction, so CI is the authoritative packaged-browser environment. Remaining manual browser work is hands-on keyboard/browser-shortcut behavior and assistive-technology review; Windows OS scaling and physical input remain separate gates.

CI run 212 (`33148840376`) extends the same release evidence with a validated 34,567,550-byte portable Windows ZIP containing only `Market of Ash/market-of-ash.exe`. The workflow clean-extracts the archive outside the checkout and uses that extracted executable for its headless launch and inspected GUI frame. Manual release-workflow run `33149086304` independently passed the same packaging, resource, GUI, manifest, and checksum checks at branch head `8922a9e`.

CI run 217 (`33151258079`) passed all nine jobs at PR head `c207161` and upgrades the Web evidence from announcement-only semantics to an actionable HTML mirror. Main Menu, Shop, Departure, route-event, arrival, Pause, and destructive-confirmation primary/safety actions publish exact labels, enabled states, and linked descriptions in an `Available game actions` region. At 960×540 the harness focuses each enabled mirror button, verifies that the region becomes visible, and sends Enter; at 1280×720 it retains independent canvas-pointer traversal. Both paths invoke the existing Godot controls and command handlers.

Downloaded manifest-v4 artifacts from Chrome, Firefox, and Edge each contain 28 captures and 38 minimum-viewport semantic-action instances. DOM snapshots preserve the action region, current screen, enabled state, and `aria-describedby` relationship. Visual inspection of the constrained Chrome Main Menu and Edge route-event frames confirms the mirror is hidden again after automated activation and does not cover the game. This automation proves browser exposure and keyboard routing, but it does not replace hands-on NVDA/Narrator testing; selectors, quantities, remapping controls, and dynamic settlement actions remain canvas-only.

CI run 220 (`33152425997`) passed all nine jobs at PR head `f6ddf4a` and extends the mirror with native Shop and Departure form fields. Each manifest-v5 browser artifact contains 28 captures, 38 minimum-viewport action instances, and 16 Shop/Departure control snapshots. Chrome, Firefox, and Edge agree on all seven cargo options, the three Ashgate destinations, legal route options, selected values, integer quantity bounds, enabled states, and linked descriptions. The 960×540 flow changes Shop and forecast quantities, cargo, and destination through those fields and waits for the corresponding Godot state; the 1280×720 path remains independent canvas-pointer coverage. Presentation/remapping controls, dynamically listed settlement actions, and hands-on screen-reader behavior remain open.

CI run 224 (`33154435995`) passed all nine jobs at PR head `4b4ee4d` and extends the mirror to dynamically generated Shop actions. Each browser artifact contains 28 captures, 159 action snapshots, and 32 planning-control snapshots across both viewports. At 960×540 the harness focuses and activates the generated `accept_contract_reedwatch_water_relief_01` button, waits for the same semantic action to become disabled after Godot accepts the contract, and requires focus to remain in the semantic region across the Shop rebuild. Stable IDs also cover contract resolution, settlement opportunities, crew recruitment/assignment, the guided trade, and Shop save/load/reset/report controls. Downloaded constrained Chrome and Edge frames show the focus-only overlay is hidden before capture and leaves the game unobscured. Input remapping and hands-on screen-reader behavior remain open; Main Menu presentation checkboxes are implemented in the following candidate.

CI run 225 (`33155440504`) passed all nine jobs at PR head `9de5d5b` and adds native presentation checkboxes to the semantic mirror. Each manifest-v6 browser artifact contains 28 captures, 159 action snapshots, and 44 control snapshots across both viewports. At 960×540 the harness toggles Reduce travel motion, Large text, and Interface sounds off and on with Space, waits for exact Godot-state parity after each change, requires focus to survive the rebuilt DOM, and verifies that Start/Continue remain ahead of optional settings in semantic order. Chrome, Firefox, and Edge agree on labels, descriptions, enabled states, and default values; the standard viewport remains independent canvas-pointer coverage. Input remapping and hands-on screen-reader behavior remain open.
