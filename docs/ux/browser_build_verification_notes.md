# Browser Build Verification Notes

CI run 195 (`33141535217`) passed on commit `c2e400e`. The Windows package job imported the project, exported Windows and Web targets, structurally validated and headlessly launched the x86-64 executable, verified the complete Web payload, and uploaded `release-candidate-195` with exact provenance and SHA-256 checksums. The downloaded executable is 97,848,976 bytes with a 328,836-byte embedded PCK, and every recorded checksum revalidated locally.

The dependent Linux/Chrome job served that exact Web artifact with the required isolation headers and produced `web-render-195`: 28 screenshots covering 14 required states at exact 960×540 and 1280×720 viewports. The matrix includes Main Menu, Shop, Pause, Departure, non-mutating Return-to-Shop, arrival handoff, destination Shop, protected new-game confirmation, normal and Large text variants, Gatekeeper's Chalk, and its resolved result. Each accepted frame requires the loading overlay to clear, the app-published state to match, the canvas ARIA label and polite status region to agree with the announcement, and decoded pixels to prove material transitions.

All 28 images were inspected. Primary actions, selected values, resource totals, map labels, event context, disabled explanations, arrival handoff, and confirmation choices remain readable at both sizes. The audit exposed and fixed Web-specific issues that source tests had not reached: browser-reserved navigation keys, stale pre-layout target geometry, stale destination diagnostics, and a focused route response left below the scroll viewport. The final route-event captures visibly reveal the first response and the result captures keep `Enter Brine Cross` on-screen.

The capture uses trusted pointer actions against app-published control geometry for packaged-browser traversal; deterministic Godot suites separately prove keyboard and controller focus semantics. Local Selenium remains unable to start Chrome because of the host's macOS Mach-port restriction, so Linux CI is the authoritative packaged-browser environment. Remaining manual browser work is cross-engine checking in current Chrome, Edge, and Firefox; Windows OS scaling and physical input remain separate gates.
