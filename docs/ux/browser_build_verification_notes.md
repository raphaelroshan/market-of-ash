# Browser Build Verification Notes

CI run 116 completed successfully with the current Node 24-based GitHub actions. Its package job imported the project, exported Windows and Web targets, verified the complete Web payload, launched the packaged Windows executable headlessly for two frames, and uploaded `release-candidate-116` with a source snapshot, provenance manifest, and verified SHA-256 integrity list. A dependent Selenium/Chrome job served that exact Web artifact with the required isolation headers, waited for Godot's loading overlay to clear, entered the campaign through the rendered canvas, and uploaded `web-render-116` with exact 960×540 and 1280×720 Main Menu and Settlement Shop captures plus a machine-readable capture manifest.

All four rendered screenshots were inspected. The Market of Ash title, quick-playtest premise, controls guidance, Start/Continue actions, save state, and accessibility controls remain readable at both sizes; the lower settings/binding content remains available through the visible scroll rail. The Shop preserves settlement, resource, campaign-outlook, contract, and opportunity context while keeping Cargo, Quantity, Buy, Sell, and the optional guided purchase visible without scrolling at 960×540. Both Shop columns expose visible independent scroll rails for their longer detail and action content. This proves packaged-browser startup plus the Main Menu and initial Settlement Shop layouts at the minimum and standard viewports. It does not yet prove the Departure Desk, event cards, large-text mode, high OS scaling, or physical input behavior.

The current capture workflow additionally navigates from the focused Shop cargo selector through Plan, Commit, and Enter Settlement using the declared keyboard focus order; it captures the Departure Desk, arrival handoff, and destination Shop at both viewports. A separate clean-browser pass enables Large text entirely by keyboard and captures the Main Menu, Settlement Shop, and Departure Desk at both sizes. A third keyboard-only pass buys Medicine, plans the Toll Road to Brine Cross, and captures the deterministic Gatekeeper's Chalk route-event card. Each transition decodes the screenshots and rejects superficial pixel changes. That extension has local parser/regression coverage but still awaits a packaged CI run because GitHub-hosted jobs are currently rejected before runner allocation by the account billing/spend limit.

The next manual browser check should confirm the newly automated normal-, large-text, and route-event states, plus unchanged resources after Return to Shop. High OS scaling remains separate from the in-app Large text option. Automated rows can move from configured to CI-backed evidence only after the extended workflow completes successfully and its images are inspected.
