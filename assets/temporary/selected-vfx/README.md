# Selected temporary VFX

These runtime copies are selected from the versioned CC0 Kenney asset kit. Release exports exclude the raw source packs and include this directory explicitly.

| Runtime file | Original source | SHA-256 |
|---|---|---|
| `departure-dust.pngdata` | `assets/temporary/kenney/particle-pack/smoke_03.png` | `a71f8abcac64f8d73a94625cc9a10033dbeafa7eaea750560cba0daa73fe8752` |

The `.pngdata` suffix keeps this copy independent of editor import metadata. Runtime code decodes the PNG bytes into an `ImageTexture`, matching the export-safe audio-buffer pattern.

License proof: `assets/temporary/kenney/LICENSE-particle-pack.txt` (CC0).
